# [OQ] Open Questions

Things I'm unsure about. When a question is resolved, move it to DC as a decision.

- Adopt Laravel Boost MCP for the owner's Laravel projects?
- Add tier-2 test enforcement (block finishing when changed code has no test)?
- Strip `.ts`/`.tsx` from the format/scrub hooks, or keep them for `vite.config.ts`?
