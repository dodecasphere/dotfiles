#!/usr/bin/env bash

#
# Set up node
#

doing "Installing Gulp and Gulp CLI..."
npm install --global gulp
npm install --global gulp-cli

doing "Installing Vue CLI..."
npm install --global vue-cli

doing "Installing Trash CLI (rm replacement)..."
npm install --global trash-cli

doing "Installing ESLint..."
npm install --global eslint

doing "Installing Prettier..."
npm install --global prettier
