#!/usr/bin/env bash

# Enable aliases to be sudo’ed
alias sudo='sudo '

# Print each PATH entry on a separate line
alias path='echo -e ${PATH//:/\\n}'

alias crons='crontab -l'
alias editcrons='crontab -e'

# Super user alias
alias _='sudo'
# Re-run the previous command with sudo. `fc -ln -1` prints the last command
# and works in both bash and zsh (unlike bash-only `history -p !!`).
alias please='sudo $(fc -ln -1)'
alias redo='sudo $(fc -ln -1)'

# Replace rm with message to use trash
# alias rm='echo "Nope. Use trash instead."'

# Reload settings — pick the rc file for whichever shell is running.
alias reload='if [ -n "$ZSH_VERSION" ]; then source ~/.zshrc; else source ~/.bash_profile; fi'
alias re='reload'

# Always enable colored `grep` output
# Note: `GREP_OPTIONS="--color=auto"` is deprecated, hence the alias usage.
alias grep='grep --color=auto'
alias fgrep='fgrep --color=auto'
alias egrep='egrep --color=auto'

alias sshcopy='pbcopy < ~/.ssh/id_rsa.pub'

alias ip='dig +short myip.opendns.com @resolver1.opendns.com'
alias localip="ipconfig getifaddr en0"

# Recover space by removing unavailable Xcode Simulators
alias removesims='xcrun simctl delete unavailable'

