[user]
	name = Michael Dulle
	email = michael.dulle@gmail.com
[core]
	autocrlf = input
	excludesfile = ~/.gitignore
[color]
	ui = true
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f

# Foresight repos live under ~/Projects/platform/. Everything else is personal
# (global identity above) or sets its own identity in .git/config.
[includeIf "gitdir:~/Projects/platform/"]
        path = ~/.gitconfig-foresight

# OS-specific config (e.g. the mac-only osxkeychain credential helper).
# Written by provisioning; git silently skips the include when absent.
[include]
        path = ~/.gitconfig-os
[init]
	defaultBranch = main
[push]
	autoSetupRemote = true
[pull]
	rebase = true
[fetch]
	prune = true
[rebase]
	autoStash = true
[diff]
	colorMoved = default
[rerere]
	enabled = true
[credential "https://github.com"]
	helper =
	helper = !gh auth git-credential
[credential "https://gist.github.com"]
	helper =
	helper = !gh auth git-credential
